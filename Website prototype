<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>App Dev - Innovation Through Collaboration</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #00f0ff;
            --secondary: #7c3aed;
            --dark: #0a0a1a;
            --darker: #050510;
            --accent: #ff006e;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--darker);
            color: #fff;
            overflow-x: hidden;
        }

        /* Animated Background */
        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: linear-gradient(135deg, var(--darker) 0%, var(--dark) 100%);
        }

        .bg-animation::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at 20% 50%, rgba(124, 58, 237, 0.1) 0%, transparent 50%),
                        radial-gradient(circle at 80% 80%, rgba(0, 240, 255, 0.1) 0%, transparent 50%);
            animation: drift 20s ease-in-out infinite;
        }

        @keyframes drift {
            0%, 100% { transform: translate(0, 0); }
            50% { transform: translate(-50px, -50px); }
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(10, 10, 26, 0.8);
            backdrop-filter: blur(10px);
            padding: 1.5rem 5%;
            z-index: 1000;
            border-bottom: 1px solid rgba(0, 240, 255, 0.2);
        }

        nav ul {
            list-style: none;
            display: flex;
            justify-content: center;
            gap: 3rem;
            flex-wrap: wrap;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            font-size: 1rem;
            font-weight: 500;
            position: relative;
            transition: color 0.3s;
        }

        nav a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--primary);
            transition: width 0.3s;
        }

        nav a:hover {
            color: var(--primary);
        }

        nav a:hover::after {
            width: 100%;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 0 5%;
        }

        .hero-content h1 {
            font-size: clamp(2.5rem, 8vw, 5rem);
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, var(--primary), var(--secondary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: glow 3s ease-in-out infinite;
        }

        @keyframes glow {
            0%, 100% { filter: brightness(1); }
            50% { filter: brightness(1.3); }
        }

        .hero-content p {
            font-size: clamp(1rem, 2vw, 1.3rem);
            color: #aaa;
            max-width: 700px;
            margin: 0 auto 2rem;
        }

        .cta-button {
            display: inline-block;
            padding: 1rem 2.5rem;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: #fff;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            transition: transform 0.3s, box-shadow 0.3s;
            box-shadow: 0 0 20px rgba(0, 240, 255, 0.3);
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 0 40px rgba(0, 240, 255, 0.6);
        }

        /* Section Styles */
        section {
            padding: 5rem 5%;
            max-width: 1400px;
            margin: 0 auto;
        }

        h2 {
            font-size: clamp(2rem, 5vw, 3rem);
            margin-bottom: 3rem;
            text-align: center;
            color: var(--primary);
        }

        /* About Section */
        .about-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .founder-card {
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.18);
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            transition: transform 0.3s, box-shadow 0.3s;
            box-shadow: 0 8px 32px 0 rgba(0, 240, 255, 0.1);
        }

        .founder-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 40px rgba(0, 240, 255, 0.3);
            border-color: var(--primary);
        }

        .founder-card h3 {
            color: var(--primary);
            margin-bottom: 0.5rem;
            font-size: 1.5rem;
        }

        .founder-card p {
            color: #aaa;
            font-size: 1rem;
        }

        /* Trackways Section */
        .trackways-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .track-card {
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.18);
            border-radius: 20px;
            padding: 2.5rem;
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
            box-shadow: 0 8px 32px 0 rgba(124, 58, 237, 0.1);
        }

        .track-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transition: left 0.5s;
        }

        .track-card:hover::before {
            left: 100%;
        }

        .track-card:hover {
            transform: scale(1.05);
            box-shadow: 0 15px 50px rgba(0, 240, 255, 0.4);
        }

        .track-card h3 {
            color: var(--primary);
            font-size: 1.8rem;
            margin-bottom: 1rem;
        }

        .track-card p {
            color: #ccc;
            line-height: 1.6;
        }

        /* Projects Section */
        .projects-container {
            text-align: center;
            padding: 3rem;
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.18);
            box-shadow: 0 8px 32px 0 rgba(0, 240, 255, 0.1);
        }

        .projects-container p {
            font-size: 1.2rem;
            color: #aaa;
            margin-bottom: 2rem;
        }

        /* Contact Section */
        .contact-form {
            max-width: 600px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            padding: 3rem;
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.18);
            box-shadow: 0 8px 32px 0 rgba(0, 240, 255, 0.1);
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--primary);
            font-weight: 500;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 1rem;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(0, 240, 255, 0.3);
            border-radius: 10px;
            color: #fff;
            font-family: inherit;
            transition: border-color 0.3s;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 15px rgba(0, 240, 255, 0.2);
        }

        .form-group textarea {
            min-height: 150px;
            resize: vertical;
        }

        .submit-btn {
            width: 100%;
            padding: 1rem;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            border-radius: 50px;
            color: #fff;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(0, 240, 255, 0.4);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 2rem;
            background: rgba(10, 10, 26, 0.8);
            border-top: 1px solid rgba(0, 240, 255, 0.2);
            color: #aaa;
        }

        @media (max-width: 768px) {
            nav ul {
                gap: 1.5rem;
            }
            
            section {
                padding: 3rem 5%;
            }
        }
    </style>
</head>
<body>
    <div class="bg-animation"></div>

    <nav>
        <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="#about">About Us</a></li>
            <li><a href="#trackways">Trackways</a></li>
            <li><a href="#projects">Projects</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </nav>

    <section id="home" class="hero">
        <div class="hero-content">
            <h1>App Dev</h1>
            <p>Empowering innovation through student-driven technology solutions. We bridge the gap between ambitious projects and limited resources.</p>
            <a href="#contact" class="cta-button">Get Started</a>
        </div>
    </section>

    <section id="about">
        <h2>About Us</h2>
        <p style="text-align: center; max-width: 800px; margin: 0 auto 3rem; color: #ccc; font-size: 1.1rem;">
            App Dev is a student-led initiative that connects companies, teams, and clients with passionate IT professionals. We specialize in delivering high-quality projects in software engineering, data science, machine learning, and cybersecurity—transforming resource constraints into opportunities for innovation.
        </p>
        <div class="about-grid">
            <div class="founder-card">
                <h3>Harshith Motipalli</h3>
                <p>Founder</p>
            </div>
            <div class="founder-card">
                <h3>Manpreet Singh</h3>
                <p>Co-Founder</p>
            </div>
            <div class="founder-card">
                <h3>Omer Rohani</h3>
                <p>Co-Founder</p>
            </div>
        </div>
    </section>

    <section id="trackways">
        <h2>Our Trackways</h2>
        <div class="trackways-grid">
            <div class="track-card">
                <h3>📊 Data Science</h3>
                <p>Extracting meaningful insights from complex datasets through statistical analysis, predictive modeling, data visualization, and advanced analytics to drive data-driven decision making.</p>
            </div>
            <div class="track-card">
                <h3>🤖 Machine Learning</h3>
                <p>Building intelligent systems with predictive analytics, neural networks, NLP solutions, and custom AI models tailored to solve your unique business challenges.</p>
            </div>
            <div class="track-card">
                <h3>🔐 Cybersecurity</h3>
                <p>Protecting digital assets through penetration testing, security audits, vulnerability assessments, and implementing robust security frameworks for your infrastructure.</p>
            </div>
            <div class="track-card">
                <h3>💻 Software Engineering</h3>
                <p>Developing scalable applications, web platforms, mobile apps, and custom software solutions using modern frameworks and best practices in full-stack development.</p>
            </div>
        </div>
    </section>

    <section id="projects">
        <h2>Previous Projects</h2>
        <div class="projects-container">
            <p>Our portfolio is growing! We're currently working on exciting projects across cybersecurity, machine learning, and data analytics.</p>
            <p style="color: var(--primary);">Check back soon to see our completed work and success stories.</p>
        </div>
    </section>

    <section id="contact">
        <h2>Contact Us</h2>
        <form class="contact-form" onsubmit="handleSubmit(event)">
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" id="name" name="name" required>
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="company">Company/Organization</label>
                <input type="text" id="company" name="company">
            </div>
            <div class="form-group">
                <label for="message">Project Details</label>
                <textarea id="message" name="message" required></textarea>
            </div>
            <button type="submit" class="submit-btn">Send Message</button>
        </form>
    </section>

    <footer>
        <p>&copy; 2025 App Dev. Innovation Through Collaboration.</p>
    </footer>

    <script>
        // Smooth scrolling with offset for fixed nav
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const target = document.querySelector(targetId);
                if (target) {
                    const navHeight = document.querySelector('nav').offsetHeight;
                    const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - navHeight;
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Form submission
        function handleSubmit(e) {
            e.preventDefault();
            alert('Thank you for your interest! We will get back to you soon.');
            e.target.reset();
        }
    </script>
</body>
</html>
